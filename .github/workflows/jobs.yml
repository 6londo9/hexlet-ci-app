jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: make setup
      - run: make start
  lint:
    needs: build
    steps:
      - run: make lint
  tests:
    needs: lint
    steps:
      - run: make test
